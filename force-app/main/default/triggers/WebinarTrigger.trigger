trigger WebinarTrigger on webinar__c (after insert) {
 /*   for(webinar__c web : trigger.new){
         AutoLeadHandler.createEvent(web.WebinarTime__c,'testing','naveen.k@mvclouds.com',web.Id);
       }  */
    }